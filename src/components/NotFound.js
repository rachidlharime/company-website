function NotFound() {
    return (
        <div className="NotFound">
            <h1 className='text-uppercase mt-5'>Page not found!</h1>
            <p>Make sure you're typing the right url</p>
            <a href="/">back to home</a>
        </div>
    );
}

export default NotFound;